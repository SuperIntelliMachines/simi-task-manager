# Database Module Schemas

This directory is for reference schema files grouped by product area.

Examples:

- `platform.sql`
- `channels.sql`
- `agents.sql`
- `insurance.sql`
- `admin.sql`

The deployment pipeline applies files from `database/migrations`, not from this directory.

