/**
 * @deprecated Admin Console now uses the shared AppShell / WorkspaceShell.
 * Kept as a thin alias so any remaining imports keep working.
 */
export { AppShell as PlatformShell } from "../layout/app-shell";
