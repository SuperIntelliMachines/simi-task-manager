/**
 * Shared SIMI application shell.
 * Implementation lives in WorkspaceShell — Insurance design is the single source of truth.
 */
export { WorkspaceShell as AppShell, AppLayout, WorkspaceShell } from "./WorkspaceShell";
